<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Car List</title>
  </head>
  <body>
    <h1>Your Car Collection</h1>

    <% if (cars.length > 0) { %>
    <ul>
      <% cars.forEach(car => { %>
      <li>
        <a href="/car/<%= car._id %>"><%= car.name %></a> - <%= car.brand %> (<%= car.year %>)
        <% if (car.isElectric) { %>
        - Electric
        <% } %>
        <a href="/car/<%= car._id %>/edit">Edit</a> |
        <form action="/car/<%= car._id %>?_method=DELETE" method="POST"> 
            <button type="submit" onclick="return confirm('Are you sure you want to delete this car?')">Delete</button>
        </form>      
        </li>
      <% }); %>
    </ul>
    <a href="/car/new">Add a car!</a></p>
    <% } else { %>
    <p>No cars added yet. <a href="/car/new">Add a car!</a></p>
    <% } %>
  </body>
</html>